# MLX Dependencies for Local Testing (Apple Silicon M4)
# For inference, data validation, and LoRA fine-tuning experiments
#
# Usage:
#   pip install -r mlx/requirements_mlx.txt
#   pip install git+https://github.com/pcuenca/mlx-vlm.git@smolvlm

# Core MLX
mlx>=0.18.0
mlx-lm>=0.18.0

# PyTorch (for torchvision video processing)
torch>=2.1.0
torchvision>=0.16.0

# Image processing
pillow>=10.0.0

# Data handling
numpy>=1.24.0
datasets>=2.18.0
transformers>=4.40.0

# Utilities
tqdm
safetensors
sentencepiece

# Note: decord does NOT work on Apple Silicon
# torchvision.io is used for video processing instead (already included above)

# SmolVLM2 support (install separately):
# pip install git+https://github.com/pcuenca/mlx-vlm.git@smolvlm
