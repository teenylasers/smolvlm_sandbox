# SmolVLM2 Training Pipeline Dependencies
# For GPU cluster training (Linux/CUDA)
#
# Usage:
#   Cloud/GPU:  pip install -r requirements.txt
#   Local/MLX:  pip install -r mlx/requirements_mlx.txt

# Core ML
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
datasets>=2.18.0

# Training utilities
trl>=0.8.0
peft>=0.10.0
bitsandbytes>=0.43.0
deepspeed>=0.14.0

# Flash attention (requires CUDA)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# Vision & Video processing (Linux only)
decord>=0.6.0  # Does NOT work on Apple Silicon - use mlx/requirements_mlx.txt instead
pillow>=10.0.0
timm>=0.9.0
einops>=0.7.0

# Utilities
num2words
numpy>=1.24.0
safetensors
sentencepiece

# Logging & Monitoring
wandb
tensorboard

# Development (optional)
pytest>=7.0
black
ruff
